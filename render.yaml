services:
  - type: web
    name: sudoku-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npx tsx index.ts
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /health
